import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ResultlanjutanPage } from './resultlanjutan.page';

describe('ResultlanjutanPage', () => {
  let component: ResultlanjutanPage;
  let fixture: ComponentFixture<ResultlanjutanPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ResultlanjutanPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
